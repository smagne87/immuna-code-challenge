const SearchText = (): JSX.Element => (
  <input
    type="search"
    className="text-field px-3 py-1 bg-white border border-gray-100 w-80"
    placeholder="Search"
  />
);

export default SearchText;
